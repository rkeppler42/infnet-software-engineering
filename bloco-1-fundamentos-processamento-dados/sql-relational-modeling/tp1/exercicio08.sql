-- tabela alunos:
-- nome do aluno
-- curso
-- email

-- tabela disciplinas
-- nome professor (se só utilizarmos as 3 tabelas do enunciado, mas mesmo assim acho que não se encaixaria completamente em nenhuma das 3)
-- disciplina

-- tabela matriculas
-- semestre (se só utilizarmos as 3 tabelas do enunciado)
-- nota

-- tabela turmas
-- semestre (se for para utilizar as 5 tabelas ao invés das 3)

-- tabela professores
-- nome professor (se for para utilizar as 5 tabelas ao invés das 3)